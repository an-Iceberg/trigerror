out_dir = "."
interfaces = wlan0, eth0, lo
; interfaces = enp0s13f0u3u2i5
protocols = ptp, http
# TODO: this should be a BPF compliant string
# TODO: test dns
; filter = tcp
count_before = 200
time_before = 1000
count_after = 200
time_after = 1000
file_size = 1
retrigger = false
; max_retriggers = 3
; TODO: max file size (calculate from packets captured)
